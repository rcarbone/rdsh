/*
 * -=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=
 *                      _     _
 *              _ __ __| |___| |__
 *             | '__/ _` / __| '_ \
 *             | | | (_| \__ \ | | |
 *             |_|  \__,_|___/_| |_|
 *
 *             rdsh, the Redis shell
 *
 * R. Carbone (rocco@tecsiel.it)
 *   4Q 2018
 *
 * SPDX-License-Identifier: BSD-3-Clause
 *
 * -=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=
 */


/* Project headers */
#include "rdsh.h"


/* tcsh builtin extension [hdel] */
int rdsh_hdel (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [hexists] */
int rdsh_hexists (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [hget] */
int rdsh_hget (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [hgetall] */
int rdsh_hgetall (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [hincrby] */
int rdsh_hincrby (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [hincrbyfloat] */
int rdsh_hincrbyfloat (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [hkeys] */
int rdsh_hkeys (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [hlen] */
int rdsh_hlen (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [hmget] */
int rdsh_hmget (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [hmset] */
int rdsh_hmset (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [hscan] */
int rdsh_hscan (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [hset] */
int rdsh_hset (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [hsetnx] */
int rdsh_hsetnx (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [hstrlen] */
int rdsh_hstrlen (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [hvals] */
int rdsh_hvals (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

