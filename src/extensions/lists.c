/*
 * -=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=
 *                      _     _
 *              _ __ __| |___| |__
 *             | '__/ _` / __| '_ \
 *             | | | (_| \__ \ | | |
 *             |_|  \__,_|___/_| |_|
 *
 *             rdsh, the Redis shell
 *
 * R. Carbone (rocco@tecsiel.it)
 *   4Q 2018
 *
 * SPDX-License-Identifier: BSD-3-Clause
 *
 * -=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=
 */


/* Project headers */
#include "rdsh.h"


/* tcsh builtin extension [blpop] */
int rdsh_blpop (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [brpop] */
int rdsh_brpop (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [brpoplpush] */
int rdsh_brpoplpush (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [lindex] */
int rdsh_lindex (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [linsert] */
int rdsh_linsert (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [llen] */
int rdsh_llen (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [lpop] */
int rdsh_lpop (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [lpush] */
int rdsh_lpush (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [lpushx] */
int rdsh_lpushx (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [lrange] */
int rdsh_lrange (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [lrem] */
int rdsh_lrem (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [lset] */
int rdsh_lset (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [ltrim] */
int rdsh_ltrim (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [rpop] */
int rdsh_rpop (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [rpoplpush] */
int rdsh_rpoplpush (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [rpush] */
int rdsh_rpush (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

/* tcsh builtin extension [rpushx] */
int rdsh_rpushx (int argc, char * argv [])
{
  return rdsh_command (argc, argv);
}

